#include <iostream>
#include <fstream>
#include <vector>
#include <memory>
#include <iomanip>
#include <exception>
#include <algorithm>
#include <chrono>
#include <utils/OptimizedIndexSet.h>
#include <utils/TypeConfig.h>
#include <set>

int main()
{
  std::set<dftefe::size_type > indexSet ;

  dftefe::utils::OptimizedIndexSet<dftefe::size_type> optimizedSetObj(indexSet);

 return 0;
}
